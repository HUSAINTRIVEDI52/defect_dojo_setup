#!/bin/bash
# DefectDojo uses a single DB — schema handled by Django migrations.
echo "Database initialization handled by Django migrations.#!/bin/bash
# DefectDojo uses a single DB — schema handled by Django migrations.
echo "Database initialization handled by Django migrations.